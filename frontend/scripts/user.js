class user{
  constructor(location, user_id){
    this.location = {
      "lat":location.lat,
      "long":location.long
    }
    this.id = user_id
    this.usr_name = // GET usr_name from DB.
    this.email = // GET usr_name from DB.
    this.profile_pic = // GET usr_name from DB.
  }

}
